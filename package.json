{
  "name": "solidus-base-aureus",
  "version": "1.0.0",
  "description": "Aureus — Yield & Rewards ecosystem on Base L2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base": "hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
